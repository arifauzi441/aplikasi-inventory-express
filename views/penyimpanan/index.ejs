<%- include('../partials/_head') %>
        <div class="rows">
            <div class="col">
              <a href="/penyimpanan/create" class="btn-success btn btn-sm mt-3 mb-3">tambah</a>
                <table class="table">
                    <thead>
                      <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nama</th>
                        <th scope="col">kode</th>
                        <th scope="col">nama penyimpanan</th>
                      </tr>
                    </thead>
                    <tbody>
                        <% for( let i = 0; i < data.length; i++ ) { %>
                            <tr>
                              <th scope="row"><%= (i+1) %></th>
                              <td><%= data[i].nama_penyimpanan %></td>
                              <td><%= data[i].kode_penyimpanan %></td>
                              <td><%= data[i].nama_ruangan %></td>
                              <td>
                                <a href="/penyimpanan/edit/<%= data[i].id_penyimpanan %>" class="btn btn-warning btn-sm">Edit</a>
                                <a href="/penyimpanan/delete/<%= data[i].id_penyimpanan %>" class="btn btn-danger btn-sm">Delete</a>
                              </td>
                            </tr>
                        <% } %>
                    </tbody>
                  </table>
            </div>
        </div>
<%- include('../partials/_foot') %>